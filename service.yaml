apiVersion: v1
kind: Service
metadata:
  name: kubedemoapp-service
spec:
  selector:
    app: kubedemoapp
  ports:
    - protocol: TCP
      port: 5000        # Port exposed by the service
      targetPort: 5000  # Port of the container
      nodePort: 30088   # NodePort for external access
  type: NodePort